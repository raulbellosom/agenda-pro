# ===========================================
# Appwrite Function: cron-expire-invitations
# ===========================================
# CRON Schedule: 0 * * * * (every hour) or */30 * * * * (every 30 min)
# Configure this in Appwrite Console > Functions > Settings > Schedule

# Appwrite Connection (Required)
APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=your_project_id
APPWRITE_API_KEY=your_server_api_key

# Database (Required)
APPWRITE_DATABASE_ID=agenda_pro_db

# Collections (Required)
COLLECTION_GROUP_INVITATIONS_ID=group_invitations
COLLECTION_AUDIT_LOGS_ID=audit_logs

# Processing (Optional)
BATCH_SIZE=100
